<Window x:Class="Durchsucher.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Durchsucher"
        mc:Ignorable="d"
        Title="Durchsucher" Height="500" Width="800" Closed="Window_Closed">
    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"></RowDefinition>
                <RowDefinition></RowDefinition>
            </Grid.RowDefinitions>
            <StackPanel Orientation="Horizontal">
                <CheckBox x:Name="cbInvert" Margin="0,0,10,0" Checked="cbInvert_Checked" Unchecked="cbInvert_Checked">invert</CheckBox>
                <TextBlock>Filter:</TextBlock>
                <TextBox x:Name="tbFilter" Grid.Row="0" TextChanged="TextBox_TextChanged" Width="359"></TextBox>
                <TextBlock Text="{Binding Path=FilteredEntries.Count}"></TextBlock>
            </StackPanel>
            <DataGrid Grid.Row="1" ItemsSource="{Binding Path=FilteredEntries}" IsReadOnly="True" MouseDoubleClick="DataGrid_MouseDoubleClick"></DataGrid>
        </Grid>
    </Grid>
</Window>
